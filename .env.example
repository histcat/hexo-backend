# hexo-backend 环境变量
# 本地开发：复制此文件为 .env（已加入 .gitignore，不会提交）
# Vercel：在 Project Settings -> Environment Variables 中添加

# JWT 密钥（必填）：用于签发会话 JWT 并加密 GitHub Token
# 至少 32 位随机字符串；修改后所有已登录会话立即失效
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars

# 本地 API 端口（可选，默认 8000，与 client/vite.config.ts 的 /api 代理一致）
# PORT=8000
