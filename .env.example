# hexo-backend 环境变量
# 复制此文件为 .env 并填入实际值

# JWT 密钥（必填 — 用于签发会话 JWT 和加密 GitHub Token）
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars

# 设为 production 时，Deno 服务器会托管构建后的前端静态资源
# 本地开发时不要设置，让 Vite dev server 处理前端
# DENO_ENV=production
